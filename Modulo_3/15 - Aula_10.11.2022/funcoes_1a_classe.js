function somar(x, y) {
  return x + y
}

const somador = somar

console.log(somar(2, 3))
console.log(somador(3, 2))

const subtrator = function (x, y) {
  return x - y
}

console.log(subtrator(20, 5))
