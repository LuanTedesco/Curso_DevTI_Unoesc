salario_hora = int(input("Digite o salário por hora "))

horas_trabalhadas = int(input("Digite a quantidade de horas trabalhadas "))

salario = int(salario_hora * horas_trabalhadas)

print(f"Seu salário é de R$ {salario}")
